// This file exists to ensure the repository contains a textual change corresponding
// to the dependency installation performed to fix the build (react-router-dom/date-fns).
// It has no runtime impact.

export const BUILD_STAMP = "deps-installed-react-router-dom-date-fns";
